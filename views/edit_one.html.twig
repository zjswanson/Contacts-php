<html>
<head>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <title>Contact List</title>
</head>
<body>
    <div class='container'>
        <div class='well'>
            <h1>Create More Contacts</h1>
        </div>
        <div class='row'>
            <div class='col-md-5'>
                <h2>Edit contact Details:</h2>
                <form action='/create' method='post'>
                    <div class='form-group'>
                        <label for='contact-name'>Enter Contact Name:</label>
                        <input type='text' name='contact-name' class='form-control' placeholder={{contact.getContactName}}>
                    </div>
                    <div class='form-group'>
                        <label for='phone'>Enter Contact Phone Number:</label>
                        <input type='text' name='phone' class='form-control' placeholder={{contact.getPhone}}>
                    </div>
                    <div class='form-group'>
                        <label for='address'>Enter Contact Address:</label>
                        <input type='text' name='address' class='form-control' placeholder={{contact.getAddress}}>
                    </div>
                    <button type='submit' class='btn'>Create Contact</button>
                </form>
            </div>
            <div class='col-md-7'>
                    {% if contact is not empty%}
                    <h2>Contact to Edit:</h2>
                        <ul>
                            <li> {{contact.getContactName}} </li>
                            <ul>
                                <li> {{contact.getPhone}} </li>
                                <li> {{contact.getAddress}} </li>
                            </ul>
                        </ul>
                    {% else %}
                        <h2>How'd you get here?:</h2>
                        <p>You must have been snooping around the site.  Use the nav buttons to go back, and come here through the buttons on the edit menu.</p>
                    {% endif %}
                <a href='/'>
                    <button class='btn'>Back to Contact List</button>
                </a>
            </div>
        </div>
    </div>
</body>
</html>
